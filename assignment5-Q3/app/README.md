For this part of the assignment, I've worked on building an app with 4+ destinations that mimic a 'tour' of a city. Users are  able to 
navigate deeper into screens and back out while maintaining correct stack behavior.

Q3 Explore Boston | Here were the following requirements, listed below: 

Requirements:
• Start with Home screen (Intro)
• Categories screen (e.g., Museums, Parks, Restaurants)
• List screen (e.g., “All Museums”)
• Detail screen (e.g., “MIT Museum”)
• Use arguments to pass the selected category and location ID down the navigation path
• Implement stack management such that going 'Home' clears the stack (popUpTo with inclusive = true)
• Use both String and Int arguments in route declarations (NavType.IntType)
• Clearly demonstrate use of NavController.navigate() with structured route strings
• Use rememberNavController() properly and keep navigation logic clean in a separate NavGraph file
• Add a reusable TopAppBar or BottomBar across all screens; Implement logic that disables back button after reaching Home via a full navigation cycle


AI USE: 
In terms of AI use, I primarily used Professor's example as a guide, I only used AI with dependency issues, such as import stmnts or
gradle dependency issue. 